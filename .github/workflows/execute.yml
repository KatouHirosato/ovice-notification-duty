name: Execute Python App

on:
  workflow_dispatch:
  schedule:
    - cron: '0 3 * * 1-5'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: pip install -r requirements.txt
      
    - name: Execute Python app
      env:
        CLIENT_ID: ${{ secrets.CLIENT_ID }}
        CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
        OPENAI_API: ${{ secrets.OPENAI_API }}
      run: |
        source .env
        python -m app "$CLIENT_ID" "$CLIENT_SECRET" "$OPENAI_API"

    - name: Commit changes
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |  
        echo $GITHUB_TOKEN
        git add schedule.csv
        git status
        git config user.name "KatouHirosato"
        git config user.email "hirosato654@gmail.com"
        git commit -m "Update schedule.csv"
        git push